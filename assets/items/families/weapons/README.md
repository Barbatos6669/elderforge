# Weapon Item Families

Weapon `.tres` files describe tiered equippable weapon lines.

Current families:

- `one_handed_sword.tres`: first main-hand melee weapon prototype.

The runtime catalog expands each family into tiered `ItemDefinition` objects,
such as `one_handed_sword_t1` through `one_handed_sword_t8`.
